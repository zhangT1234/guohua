<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.newgrand.mapper.ComSqlMapper">

    <select id="queryObject" resultType="java.util.Map">
        ${sql}
    </select>
    <select id="queryString" resultType="java.lang.String">
        ${sql}
    </select>
    <select id="update" resultType="java.lang.Integer">
        ${sql}
    </select>
    <select id="insert" resultType="java.lang.Integer">
        ${sql}
    </select>
</mapper>